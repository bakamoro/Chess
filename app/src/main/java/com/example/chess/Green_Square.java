package com.example.chess;

public class Green_Square {
    int col//the square's column.
            ,row;//the square's row.

    /**
     * the Green_Square's constructor.
     * @param col - the square's column.
     * @param row - the square's row.
     */
    public Green_Square(int col, int row){
        this.col = col;
        this.row = row;
    }
}
